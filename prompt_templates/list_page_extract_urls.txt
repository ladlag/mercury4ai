请从这个列表页面中提取所有项目的信息：

需要提取的内容：
1. 每个项目的标题
2. 每个项目的详情页链接URL（必须是完整的绝对URL）
3. 每个项目的简短描述或预览信息
4. 发布日期或更新时间（如果有）

数据清洗要求：
1. URL必须是完整的绝对路径（包含 https:// 或 http://）
2. 如果URL是相对路径，请补全为绝对路径
3. 移除HTML标签和多余空格
4. 统一日期格式为 YYYY-MM-DD
5. 按照页面上出现的顺序排列

请以JSON格式返回结果，确保结果是有效的JSON。
